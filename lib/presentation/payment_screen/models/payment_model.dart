/// This class defines the variables used in the [payment_screen],
/// and is typically used to hold data that is passed between different parts of the application.
class PaymentModel {

  String? icon;
  String? title;
  int? id;
  PaymentModel(this.icon,this.title, this.id);
}
